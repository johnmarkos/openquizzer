{
  "name": "openquizzer",
  "private": true,
  "scripts": {
    "setup-hooks": "git config core.hooksPath .githooks",
    "prepare": "npm run setup-hooks",
    "test": "node --test openquizzer.test.js",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
